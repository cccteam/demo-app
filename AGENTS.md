# AGENTS.md — root

## Project Overview

This repo contains a Angular web app in the gui folder, and a REST server in the root folder. It makes heavy use of code generation. The unit tests start of a docker container, so must be run as root on the Jules image.
